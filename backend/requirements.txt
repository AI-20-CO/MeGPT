# FastAPI Backend
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-dotenv==1.0.0
pydantic==2.9.0

# LangChain + LLM
langchain==0.3.0
langchain-groq==0.2.0
langchain-core==0.3.0

# Utilities
pyyaml==6.0.1

# Fuzzy trigger matching (lazy-load signature takes by user intent).
# Pure C under the hood, ~1MB wheel, MIT license, no transitive deps.
rapidfuzz==3.10.1

# Test runner (dev only — used by backend/test_main.py).
pytest==8.3.3
